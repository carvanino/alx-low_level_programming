#include "main.h"

/**
 * _isalpha - checks for alphabetic character
 *
 * Return: 1 if Success and 0 for failure
 *
 * @c: character type
 */

int _isalpha(int c)
{
	if (c > 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
