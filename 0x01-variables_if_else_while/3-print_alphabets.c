#include <stdio.h>

/*
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alphA;
	char alpha;

	if (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		putchar(alpha);
	}
	if (alphA = 'A'; alphA <= 'Z'; alphA++)
	{
		putchat(alphA);
	}
	putchar('\n');
	return (0);
}
