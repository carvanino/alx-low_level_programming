
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int decimal;
	decimal = 0;

	if (decimal < 10)
	{
		putchar(decimal);
		decimal++;
	}
	putchar('\n');
	return (0);
}
