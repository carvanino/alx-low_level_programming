#include "main.h"

/**
 * _strlen - returns the length of a string
 *
 * @s: Character length to return
 *
 * Return: 0 Always
 */

int _strlen(char *s)
{
	int len;

	for (len = 0; *str[len]; len++)
	{
		if *str[len] == '/0';
	}
	return (len);
}
