#include "main.h"

/**
 * _strlen - returns the length of a string
 *
 * @s: Character length to return
 *
 * Return: 0 Always
 */

int _strlen(char *s)
{
	int len;

	for (len = 0; s[len]; len++)
	{
		if (s[len] == '\0')
		{
		}
	}
	return (len);
}
