#include "main.h"

/**
 * rev_string - a function that reverses a string
 *
 * @s: string thag will be reversed
 *
 * Return: Always 0
 */

void rev_string(char *s)
{
	int i;

	i = 0;
	while (s[i])
	{
		i++;
	}
	while (i--)
	{
		return (s[i]);
	}
}
